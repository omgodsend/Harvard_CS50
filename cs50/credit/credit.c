#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long card;

    do
    {
        card = get_long("Enter card number: ");
    }
    while (card < 0);

    while(card > 0)

    alternate = 0
    {
    long lastNumber = visa/10;
    visa = visa / 100;
    long alternate = lastNumber % 10;
    printf("%li\n", everyOther);
}
    }

    //if


   // else
  //  {
   //     printf("INVALID");
   // }

}
