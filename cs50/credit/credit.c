#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long card;

    do
    {
        card = get_long("Enter card number: ");
    }
    while (card < 0);

    while(card > 0)

    alt1 = 0
    {
    alternate + alternate
    long lastNumber = visa/10;
    visa = visa / 100;
    long alternate = lastNumber % 10;
    alternate + alt1
    return alternate
}
    }

    //if


   // else
  //  {
   //     printf("INVALID");
   // }

}
