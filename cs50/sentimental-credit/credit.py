from cs50 import get_int

def main():
    try:
        card = get_int("Number: ")
        if n > 0:
                return card
        except ValueError:
            print("Not and integer")

    if len(card) != 13 or != 15 or != 16:
         print("INVALID")


def algorithm(number)













main()